# Archive 핵심 화면 사용성·디자인 기준선 (2026-08-12)

## 확정 결과

- 대상 화면: 로그인, 대시보드, 고객사 목록, 고객사 상세, 정기점검 이력,
  회의록, 트러블슈팅, 자료실, 마이페이지
- 화면 폭: 360, 390, 768, 1024, 1440px
- 최종 기준선:
  `/opt/frog2-dev/visual-baselines/20260812-usability-final`
- 변경 전 참고 기준선:
  `/opt/frog2-dev/visual-baselines/20260812-usability-before`
- 화면에는 실제 업무 데이터가 포함될 수 있으므로 기준선 파일은 Git에 넣지 않고
  소유자 전용 권한 `0600`으로 보관한다.

## 실제 확인한 문제와 최소 조치

| 발견 문제 | 조치 | 사용자가 느끼는 변화 |
|---|---|---|
| 모바일 라이선스 차트가 좁게 압축되어 날짜와 선을 읽기 어려움 | 차트만 720px의 읽기 가능한 폭을 유지하고 내부 좌우 이동을 허용했다. 모바일 날짜 눈금은 최대 8개로 줄였다. | 그래프가 찌그러지지 않고 날짜와 추이를 구분할 수 있다. |
| 360px에서 차트 제목과 데이터 수가 서로 밀어 제목이 어색하게 줄바꿈됨 | 480px 이하에서 제목과 데이터 수를 두 줄로 배치했다. | 제목이 단어 중간에서 끊기지 않는다. |
| 모바일 트러블슈팅 표가 네 열을 강제로 압축해 고객사명과 제목이 글자 단위로 줄바꿈됨 | 표의 최소 폭을 720px로 유지하고 표 컨테이너 안에서만 좌우 이동하게 했다. | 행 높이가 과도하게 늘어나지 않고 열 관계를 읽을 수 있다. |
| 일부 자동 캡처가 이전 화면의 스크롤 위치를 이어받음 | 각 화면 캡처 직전 문서 스크롤을 `(0, 0)`으로 초기화하고 결과를 검증·기록한다. | 기준 화면이 항상 같은 위치에서 만들어져 회귀 비교를 신뢰할 수 있다. |
| 변경된 CSS·JS가 브라우저 캐시에 남을 가능성 | 자산 버전을 `20260812-usability-baseline-2`로 갱신했다. | 강제 새로고침 없이 최종 자산을 받는다. |

## 검증 증거

- 최종 캡처: 45장
- 브라우저 console error: 0
- 문서 전체 가로 overflow: 0
- `(0, 0)`이 아닌 위치에서 시작한 캡처: 0
- Gradle clean build: 최종 코드로 2회 연속 성공
- 전체 테스트: 449개 성공, 실패 0, 건너뜀 0
- WAR allowlist 검사: 성공
- Java 22 JspC: 38개 소스, 63개 클래스, 오류 0
- JavaScript 문법 검사: 성공
- `git diff --check`: 성공

## 배포와 롤백

- 개발 배포 경로: `/opt/tomcat-dev/webapps/frog2.war`
- 최종 개발 WAR SHA-256:
  `715ec7ccccaf663f1230f3b0da57954212434008ed2dff926bca078f1b534ac6`
- 직전 개발 배포 백업:
  `/opt/frog2-dev/backups/usability-final-20260812_230827`
- 작업 시작 전 개발 배포 백업:
  `/opt/frog2-dev/backups/usability-final-20260812_230404`
- 롤백 시 `tomcat-dev.service`만 중지하고 직전 백업의 WAR, exploded app,
  JSP work 디렉터리를 원래 개발 경로에 복원한 뒤 개발 서비스만 시작한다.
- 운영 `/opt/tomcat`, 운영 WAR, 8080 서비스와 DB는 변경하지 않았다.

## 기준선 사용 방법

다음 UI 변경부터는 같은 9개 화면과 5개 폭으로 캡처하고 다음을 확인한다.

1. 의도한 화면 외 변경이 없는가
2. 문서 전체 가로 overflow가 0인가
3. console error가 0인가
4. 모든 캡처가 문서 맨 위에서 시작하는가
5. 모바일의 표·차트가 페이지 전체가 아니라 자기 영역 안에서만 스크롤되는가
